/**
 * Preset job IDs for the dashboard selector. Replace values with real `jobs.id`
 * rows from your database (see Flyway seed or manual INSERT).
 */
export const JOB_ID_PRESETS = [
  {
    label: 'Example job (replace UUID in jobPresets.js)',
    value: '00000000-0000-4000-8000-000000000001',
  },
]
